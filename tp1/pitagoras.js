export default class Pitagoras {
    
    constructor(a,b)
    {
        this.opuesto = a
        this.adyasente = b
    }

    mostrar_resultado() {
        
        let h = Math.pow(Number(this.a),2 + Number(this.b),2);
    }

    
}